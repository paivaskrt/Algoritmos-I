#include <iostream>
#include <cmath>
#include <iomanip>

using namespace std;

int main ()
{
   int a;
   int b;
   int c;
   int d;
   int x;
   int y;
  
   
   x = c + d;
   y = a + b;
   
   
   cin >> a >> b >> c >> d;
   
   if ( ( b > c ) && ( d > a ) && ( c + d > a + b ) && ( c > 0 ) && ( d > 0 ) && ( a %2 == 0 ))
   
   cout << "Valores aceitos" << endl;
   
   else 
   
   cout << "Valores nao aceitos" << endl;
   
  
	return 0;
	
}
